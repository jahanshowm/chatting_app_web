# adminchat.kr — 관리자 CMS + API 프록시
# Flutter Web: /var/www/adminchat
# NestJS API: 127.0.0.1:3001 (/api, /uploads, /photos)

server {
    listen 80;
    listen [::]:80;
    server_name adminchat.kr www.adminchat.kr;

    # certbot 인증 후 아래 redirect 블록 활성화
    # return 301 https://$host$request_uri;

    root /var/www/adminchat;
    index index.html;

    client_max_body_size 20m;

    # Flutter SPA
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /photos/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
}

# SSL 적용 후 certbot이 생성하는 server { listen 443 ssl; ... } 블록을 사용하세요.
# 수동 HTTPS 예시:
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name adminchat.kr www.adminchat.kr;
#
#     ssl_certificate     /etc/letsencrypt/live/adminchat.kr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/adminchat.kr/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     root /var/www/adminchat;
#     index index.html;
#     client_max_body_size 20m;
#
#     location / { try_files $uri $uri/ /index.html; }
#     location /api/ {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#     location /uploads/ { proxy_pass http://127.0.0.1:3001; proxy_set_header Host $host; }
#     location /photos/  { proxy_pass http://127.0.0.1:3001; proxy_set_header Host $host; }
# }
